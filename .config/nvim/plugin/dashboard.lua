-- This is a config for the dashboard that comes with folke/snacks. Not currently enabled.

return {
    sections = {
        { section = "header" },
        { section = "keys", gap = 1, padding = 1 },
    },
}
