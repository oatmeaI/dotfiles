:: yabaimode @ : sketchybar --set skhd label="SKHD: WINDOW" && sketchybar --add item hint center && sketchybar --set hint label="L/R to move side by side; <space> to fullscreen; <enter> to align all windows; f/s/b for float/stack/bsp"
:: default : sketchybar --set skhd label="SKHD: NORMAL" && sketchybar --remove hint

hyper - d ; yabaimode 
yabaimode < escape ; default 


# Grid: <rows>:<cols>:<start-x>:<start-y>:<width>:<height>
yabaimode < return      ; default : yabai -m space --layout stack && yabai -m space --layout float 
yabaimode < b           ; default : yabai -m space --layout bsp
yabaimode < f           ; default : yabai -m space --layout float
yabaimode < s           ; default : yabai -m space --layout stack
yabaimode < space       ; default : yabai -m space --layout float && yabai -m window --grid 1:1:0:0:1:1
yabaimode < left        : yabai -m space --layout float && yabai -m window --grid 1:2:0:0:1:1
yabaimode < right       : yabai -m space --layout float && yabai -m window --grid 1:2:1:0:1:1


# yabaimode < space       : ~/.config/skhd/toggle_layout.py
# yabaimode < left     : yabai -m window --stack west
# yabaimode < right    : yabai -m window --stack east

# Unstack
# TODO: could add a script here that checks to see if we should unstack or warp?
# yabaimode < hyper - left     : yabai -m window --insert west && yabai -m window --toggle float && yabai -m window --toggle float
# yabaimode < hyper - right    : yabai -m window --insert east && yabai -m window --toggle float && yabai -m window --toggle float
